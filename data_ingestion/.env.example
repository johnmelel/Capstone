# Google Cloud Storage Configuration
GCS_BUCKET_NAME=your-bucket-name
GCS_BUCKET_PREFIX=documents  # Optional: folder/prefix in bucket (leave empty for root)
GOOGLE_SERVICE_ACCOUNT_JSON=service_account.json
GCS_RECURSIVE=true  # Set to false to only process from prefix folder, not subfolders

# Google Cloud Vertex AI Configuration (for multimodal embeddings)
GOOGLE_CLOUD_PROJECT=your-project-id
GOOGLE_CLOUD_LOCATION=us-central1  # or your preferred location

# Milvus Configuration
MILVUS_URI=your-milvus-uri
MILVUS_API_KEY=your-api-key
MILVUS_COLLECTION_NAME=pdf_embeddings

# Embedding Configuration
# Using Vertex AI multimodal embeddings (1408 dimensions)
EMBEDDING_DIMENSION=1408

# Processing Configuration
CHUNK_SIZE=800  # Characters per chunk
CHUNK_OVERLAP=150  # Overlap between chunks
BATCH_SIZE=100

# Output Directory
OUTPUT_DIR=./extracted_content
